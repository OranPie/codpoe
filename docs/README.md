# PoeCoder Docs Index

Current docs state:

- `commands.md`: model-callable runtime tool catalog (args and effects).
- `context-flow.md`: turn protocol, default context inclusion/exclusion, and tool-result forwarding modes.

Release advisory:

- `v0.1.1` is unsuggested due to performance, higher cost, and stability issues.
- Prefer `v0.1.2+`.
